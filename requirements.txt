# SUMM-Lens dependencies
# Install: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# (Drop the -i flag if you are outside mainland China.)
#
# Verified against the project's existing venv (see venv/Lib/site-packages/*-dist-info).
# Pins use upper bounds where API drift would matter and floors elsewhere.

# ── Core ──────────────────────────────────────────────────────────────
torch>=2.1.0,<2.9
transformers>=4.40.0,<4.60
datasets>=2.16.0,<5.0
accelerate>=0.26.0,<2.0
huggingface_hub>=0.25.0,<1.0
sentencepiece>=0.2.0
protobuf>=4.21.0
safetensors>=0.4.0

# ── Evaluation ────────────────────────────────────────────────────────
rouge-score>=0.1.2
bert-score>=0.3.13
nltk>=3.9.0

# ── Visualization & analysis ──────────────────────────────────────────
matplotlib>=3.7.0,<4.0
seaborn>=0.12.0
pandas>=2.0.0
numpy>=1.24.0,<3.0
scikit-learn>=1.3.0

# ── Utilities ─────────────────────────────────────────────────────────
tqdm>=4.65.0

# ── Notebook ──────────────────────────────────────────────────────────
ipykernel>=6.0.0
ipywidgets>=8.0.0
